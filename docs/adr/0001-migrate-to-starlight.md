# Migrate to Starlight

The Knowledge Base is moving from Docusaurus to Starlight on Astro while preserving the public deployment path `/knowledge-base/` and the existing locale model: German at the root, English under `/en/`. This keeps existing URLs stable while replacing the documentation framework and its Docusaurus-specific content structure.
